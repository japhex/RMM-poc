export type DataType = Policy | Job | Device | Site

type Policy = {
  id: number
  name: string
}

type Job = {
  id: number
  name: string
}

type Device = {
  id: number
  name: string
}

type Site = {
  id: number
  name: string
}
